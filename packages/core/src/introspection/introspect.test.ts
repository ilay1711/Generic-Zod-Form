// Tests for the introspection module are split across:
//   scalars.test.ts    — scalar types, optional/nullable, defaults, transforms, labels, meta, string checks
//   enum-union.test.ts — ZodEnum, ZodNativeEnum, ZodDiscriminatedUnion
//   object.test.ts     — ZodObject, nested objects, deeply nested optional
//   array.test.ts      — ZodArray, array of objects, min/max constraints
